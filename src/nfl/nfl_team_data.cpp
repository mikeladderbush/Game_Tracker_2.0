#include "nfl_teams.h"

// TODO (NFL_SUPPORT_ROADMAP.md step 10): 32 teams' worth of sprite pattern +
// palette data goes here, mirroring nba/nba_team_data.cpp's
// `static const uint8_t X_pattern[1024] = {...}; static const uint16_t
// X_palette[N] = {...}; const TeamSprite x = {"Name", x_pattern, x_palette,
// 32, 32};` shape per team. Deliberately not fabricated - this is real
// pixel-art/palette authoring work, not something to guess at.
